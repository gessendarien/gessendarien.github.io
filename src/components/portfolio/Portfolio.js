import React from 'react';
import PortfolioBlock from "./PortfolioBlock";

export default function Portfolio({innerRef, darkMode}) {
    return (
            <PortfolioBlock darkMode={darkMode}/>
    );
};